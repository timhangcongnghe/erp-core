<div class="top-menu">
    <ul class="nav navbar-nav pull-right">
        <!-- BEGIN NOTIFICATION DROPDOWN -->
        <!-- DOC: Apply "dropdown-dark" class after "dropdown-extended" to change the dropdown styte -->
        <!-- DOC: Apply "dropdown-hoverable" class after below "dropdown" and remove data-toggle="dropdown" data-hover="dropdown" data-close-others="true" attributes to enable hover dropdown mode -->
        <!-- BEGIN LANGUAGE BAR -->
        <!-- DOC: Apply "dropdown-dark" class after below "dropdown-extended" to change the dropdown styte -->
        <li class="dropdown dropdown-language">
            <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown" data-close-others="true" aria-expanded="true">
                <img alt="" src="/backend/assets/global/img/flags/vn.png">
                <span class="langname">Tiếng Việt</span>
                <i class="fa fa-angle-down"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-default">
                <li>
                    <a href="javascript:;">
                        <img alt="" src="/backend/assets/global/img/flags/vn.png">Tiếng Việt
                    </a>
                </li>
                <!--<li>
                    <a href="javascript:;">
                        <img alt="" src="/backend/assets/global/img/flags/us.png">English
                    </a>
                </li>-->
            </ul>
        </li>
        <!-- END LANGUAGE BAR -->
        <!-- BEGIN USER LOGIN DROPDOWN -->
        <!-- DOC: Apply "dropdown-dark" class after below "dropdown-extended" to change the dropdown styte -->
        <li class="dropdown dropdown-user">
            <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown" data-close-others="true">
                <% if current_user.avatar.present? %>
                    <%= image_tag current_user.avatar.system, class: "img-circle", alt: current_user.email %>
                <% else %>                    
                    <%= image_tag "/backend/img/placeholder.jpg", class: "img-circle", alt: current_user.email %>
                <% end %>
                <span class="username username-hide-on-mobile"> <%= current_user.display_name %> </span>
                <i class="fa fa-angle-down"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-default">
                <li>
                    <%= link_to '<i class="icon-user"></i>'.html_safe + t('profile'),
                        erp.profile_backend_accounts_path,
                        redirect_to: erp.backend_path,
                        class: 'link-method'
                    %>
                </li>
                <li>
                    <%= link_to '<i class="icon-key"></i>'.html_safe + t('logout'),
                        erp.destroy_user_session_path,
                        method: :delete,
                        redirect_to: erp.backend_path,
                        class: 'link-method'
                    %>
                </li>
            </ul>
        </li>
        <!-- END USER LOGIN DROPDOWN -->        
    </ul>
</div>