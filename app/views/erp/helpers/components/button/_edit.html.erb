<% if options[:href].present? %>
    <a class="btn purple <%= options[:class] if options[:class].present? %>" href="<%= options[:href] if options[:href].present? %>">
        <i class="fa fa-edit"></i> <%= options[:text] if options[:text].present? %>
    </a>
<% else %>
    <button type="button" class="btn purple <%= options[:class] if options[:class].present? %>">
        <i class="fa fa-edit"></i> <%= options[:text] if options[:text].present? %>
    </button>
<% end %>