<a href="#remove"
    class="btn btn-danger <%= options[:class].to_s if options[:class].present? %>"
>
    <%= options[:label].present? ? options[:label] : '<i class="fa fa-close"></i>'.html_safe %>
</a>